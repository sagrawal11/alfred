# Core dependencies
Flask>=3.0.0
twilio>=3.3.6
python-dotenv>=1.0.0
APScheduler>=3.10.4
python-dateutil>=2.8.2

# HTTP requests
requests>=2.31.0

# NLP and ML dependencies (Google Gemini API)
# New SDK (preferred) - supports Gemini 2.0+
google-genai>=1.0.0
# Old SDK (fallback) - deprecated but kept for compatibility
google-generativeai>=0.3.0

# Database (Supabase)
supabase>=2.0.0

# Google Calendar API dependencies
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
google-api-python-client>=2.100.0

# Security & Authentication
cryptography>=41.0.0  # For token encryption (Fernet)
bcrypt>=4.0.0  # For password hashing
Flask-WTF>=1.1.0  # For CSRF protection
Flask-JWT-Extended>=4.5.0  # For JWT tokens (API auth)
Flask-CORS>=4.0.0  # For CORS support

# Caching (optional - Redis)
redis>=5.0.0  # For caching and message queues

# Validation
pydantic>=2.0.0  # For request/response validation

# Timezone handling
pytz>=2023.3  # For timezone support

# Rate limiting
Flask-Limiter>=3.5.0  # For API rate limiting

# Logging & Monitoring
python-json-logger>=2.0.0  # For structured logging

# Testing (development)
pytest>=7.4.0
pytest-cov>=4.1.0

# Optional: Simple bot dependencies (for basic features)
wikipedia>=1.4.0
pyowm>=2.6.1
wolframalpha>=3.0
